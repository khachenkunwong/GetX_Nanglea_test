import 'package:flutter/material.dart';

class UnknownRoutes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.center,
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            Text('UnknownRoute'),
          ],
        ),
      ),
    );
  }
}
